export enum appPaths {
  index = "/",
  searchAddress = "/address",
  table = "/table",
  calendar = "/calendar",
  geo = "/geo",
  widgets = "/widgets",
  settings = "/settings",
  profile = "/profile",
  finance = "/finance",
  logout = "/logout",
}
